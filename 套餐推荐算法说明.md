# 套餐推荐算法说明

> 本文档面向业务决策层，用通俗语言解释系统如何为每位用户选出"最合适的套餐"。

---

## 一、核心思路：一句话概括

> **在用户能接受的预算范围内，优先选一个"流量够用、价格合理"的套餐，同时尽量不让用户多花冤枉钱。**

---

## 二、推荐流程总览

整个推荐过程分为 **六大步骤**，可以类比为"去商场买衣服"：

| 步骤 | 类比 | 系统做了什么 |
|------|------|-------------|
| ① 了解用户 | 看顾客的身材、预算 | 读取用户消费数据、套餐信息、超套情况 |
| ② 计算预算上限 | 看钱包里能花多少钱 | 根据用户消费习惯算出"软预算"和"硬预算" |
| ③ 筛选候选套餐 | 从货架上挑出合适的尺码 | 去掉不匹配运营商、不满足宽带需求的套餐 |
| ④ 逐个打分 | 试穿后打分 | 从价格、流量、语音、宽带四个维度综合评分 |
| ⑤ 分桶排序 | 先分"合适/勉强/太贵"三档，档内再排名 | 预算内的排前面，分数高的排前面 |
| ⑥ 生成结果 | 导购员给建议话术 | 输出推荐理由、营销话术、预计月费 |

---

## 三、评分机制详解（核心公式）

系统给每个候选套餐算一个 **综合得分**，得分越高（越接近0）越好。

### 3.1 打分维度和权重

```
综合得分 = -(
    价格差距 × 价格权重
  + 流量缺口 × 60
  + 语音缺口 × 0.6
  + 流量浪费 × 0.5
  + 语音浪费 × 0.03
  + 宽带浪费 × 0.02
)
```

**通俗解释各维度：**

| 维度 | 含义 | 权重 | 为什么这么定 |
|------|------|------|-------------|
| **流量缺口** | 套餐给的流量不够用户用的差额 | **60**（最高！） | 流量不够→用户超套→投诉，这是最要命的 |
| **价格差距** | 套餐比当前贵了多少 | **8~16**（浮动） | 高端客户对涨价不敏感，低端客户很敏感 |
| **流量浪费** | 套餐给的流量比用户需要的多出多少 | **0.5** | 浪费一点没关系，但不能太离谱 |
| **语音缺口** | 通话分钟不够的差额 | **0.6** | 现在大家主要用微信，语音不太重要 |
| **语音浪费** | 通话分钟多出来的部分 | **0.03** | 基本忽略不计 |
| **宽带浪费** | 宽带速率超出当前需求 | **0.02** | 基本忽略不计 |

### 3.2 为什么流量权重是语音的100倍？

现实数据告诉我们：
- 用户每月平均用 **10~30GB 流量**，缺口1GB就要多花 **0.5元**
- 用户每月平均打 **100~200分钟电话**，缺口1分钟才多花 **0.1元**
- 流量超套是用户投诉的**首要原因**，语音超套几乎没人关心

所以系统把**流量覆盖**放在压倒性的第一位。

### 3.3 价格敏感度分层

不同客户对"涨价"的接受度不同，系统根据客户价值分为三档：

| 客户层级 | 判定条件 | 价格权重 | 含义 |
|---------|---------|---------|------|
| **高端客户** | 全球通用户、中高端用户、月均ARPU≥128元 | **8** | 涨价不敏感，可以推荐高档套餐 |
| **普通客户** | ARPU 59~128元 | **12** | 适度涨价可以接受 |
| **低端客户** | ARPU<59元 或 使用老旧套餐 | **16** | 对价格很敏感，涨价要谨慎 |

**举例：** 同样是套餐比当前贵20元，高端客户扣 20×8=160分，低端客户扣 20×16=320分。低端客户会更倾向选便宜的方案。

---

## 四、预算上限是怎么算的？

系统为每个用户算出两道"价格红线"：

### 4.1 软预算（舒适区）

```
软预算 = 当前消费 + 缓冲金额
```

- **没有超套**的用户：缓冲 = 5元（小幅提档即可）
- **有超套**的用户：缓冲 = 10元（需要更大空间来覆盖超套）
- 如果用户有**额外消费**（超消金>30元），再加一点弹性空间（最多+20元）
- 如果用户**账户余额充足**（>100元），再+5元

### 4.2 硬预算（极限区）

```
硬预算 = 当前价格 + 涨价上限 + 20元
```

**涨价上限**根据用户类型不同：

| 用户类型 | 基础涨价上限 | 说明 |
|---------|------------|------|
| FTTR用户 | 60元 | 高价值融合用户，接受大幅升级 |
| 宽带用户（<79元档） | 30元 | 宽带融合有一定升级空间 |
| 宽带用户（≥79元档） | 40元 | 已经是宽带高价值用户 |
| 纯手机（<29元档） | 30元 | 低价用户可以多涨一点 |
| 纯手机（29~59元） | 25元 | 中低价用户 |
| 纯手机（59~99元） | 30元 | 中档用户 |
| 纯手机（≥99元） | 40元 | 高档手机用户 |

**额外放宽条件（涨价上限可以再加）：**

| 条件 | 额外加多少 | 理由 |
|------|-----------|------|
| 长期超套（超套≥30元 或 饱和度≥80%） | +15元 | 超套严重，有强烈升级需求 |
| 轻度超套 | +5元 | 有一些升级动力 |
| 流量饱和度≥90%（但没超套） | +8元 | 快满了，预防性升级 |
| 语音饱和度≥90% | +5元 | 通话快用完了 |
| 零合约用户 | +5元 | 没有合约约束，可以灵活变更 |
| 老旧套餐用户 | +8元 | 强升级目标，运营商鼓励升级 |

---

## 五、候选套餐是怎么筛选的？

不是所有套餐都参与评分，系统会先"过筛子"：

1. **运营商匹配**：移动用户只看移动套餐；电信/联通用户只看"竞品套餐"（用于策反）
2. **宽带保底**：如果用户已经有宽带，推荐的套餐宽带速度不能比现在低
3. **价格门槛**：套餐价格不能低于用户当前价格（不能推荐降档，除非是策反场景）
4. **场景匹配**：FTTR用户优先看FTTR套餐，宽带用户看宽带融合套餐

---

## 六、分桶排序（先分档，再排名）

候选套餐先按"预算达标情况"分成5个桶：

| 桶 | 条件 | 含义 |
|----|------|------|
| **桶0** ⭐ | 价格≤软预算 且 资源全覆盖 | **最佳**：价格舒适、资源够用 |
| **桶1** | 价格≤硬预算 且 资源全覆盖 | **次优**：价格偏高但资源够用 |
| **桶2** | 价格≤软预算（但资源不够） | 价格舒适但有缺口 |
| **桶3** | 价格≤硬预算（但资源不够） | 价格偏高且有缺口 |
| **桶4** | 超过硬预算 | 太贵了，基本不会选 |

**最终排序规则：**
1. 先按桶号排（桶0最优先）
2. 同桶内按综合得分排（得分高的优先）
3. 得分相同时按价格排（便宜的优先）

---

## 七、输出内容

系统为每位用户生成：

| 输出项 | 说明 |
|--------|------|
| **最终推荐套餐** | 系统选出的最佳方案 |
| **备选方案**（最多3个） | 次优方案供人工参考 |
| **推荐理由** | 如"缓解流量超套""宽带提速""套餐焕新"等 |
| **AI营销话术** | 自动生成的外呼/短信话术 |
| **预计月费** | 套餐费 + 预估超套费 |
| **预计节省** | 相比当前ARPU能省多少 |
| **风险等级** | 低/中/高（资源不覆盖则为高风险） |

---

## 八、关键设计原则总结

1. **流量为王**：流量覆盖的权重是其他所有因素的100倍以上，宁可多给流量也绝不能让用户超套
2. **因人而异**：高端客户可以推荐更贵的套餐，低端客户要严格控价
3. **适度升级**：通过"软硬预算"机制，既鼓励合理升级，又防止过度推销
4. **安全兜底**：超出硬预算的套餐直接排到最后，避免推荐用户完全无法接受的方案
5. **透明可解释**：每个推荐都有理由和话术，一线人员能直接理解为什么推荐这个套餐

---

*文档版本：2026-06-09 | 对应代码：backend/app/services/engine.py*
