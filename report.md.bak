# 基于规则引擎的存量用户套餐推荐系统设计与实现

## ——中国移动新员工转正论文

---

## 摘要

随着国内电信市场逐步进入存量竞争阶段，如何通过精准营销提升存量用户价值成为运营商的核心课题。本文针对中国移动存量用户运营场景，设计并实现了一套基于规则引擎的套餐推荐系统。系统采用前后端分离架构，前端基于 React 19 + TypeScript 构建交互式分析看板，后端基于 FastAPI + SQLAlchemy 提供高性能 RESTful API，数据层采用 SQLite 轻量级数据库。推荐引擎通过用户分群、候选筛选、多维度加权评分等算法，自动为用户匹配最优套餐方案，并支持人工校正与 AI 营销话术生成。系统已在昌吉分公司一线营销团队中投入使用，有效提升了套餐推荐的准确性和外呼营销的工作效率。

**关键词**：存量用户运营；套餐推荐；规则引擎；FastAPI；React；前后端分离

---

## 第一章 绪论

### 1.1 研究背景与意义

近年来，我国移动通信市场用户渗透率已超过 120%，新增用户增长空间日趋饱和，三大运营商的竞争重心已从"拉新"转向"存量经营"。根据工业和信息化部发布的数据，截至 2025 年底，全国移动电话用户总数已超过 17 亿户，中国移动作为市场份额最大的运营商，面临着存量用户 ARPU 值提升困难、用户流失风险加大等挑战。

在存量用户运营实践中，"套餐提档"和"融合保有"是两项核心业务。一线营销人员需要定期对存量用户进行数据分析，识别出具有提档潜力的用户、超套用户、以及可推广宽带融合或 FTTR 全光 WiFi 的目标用户，然后通过电话回访进行精准推荐。然而，传统的分析方式主要依赖人工从系统中导出数据、使用 Excel 进行筛选和计算，存在以下痛点：

1. **效率低下**：人工逐条分析用户数据耗时巨大，一个营销团队每天能处理的用户量有限
2. **标准不统一**：不同营销人员对套餐推荐的理解和判断标准不一致，导致推荐质量参差不齐
3. **经验依赖强**：推荐结果高度依赖个人经验，缺乏系统化的分析框架
4. **话术准备不足**：一线人员需要自行准备营销话术，难以做到针对性和专业性

因此，开发一套智能化的套餐推荐系统，帮助一线营销人员快速完成用户分析、套餐匹配和话术准备，对于提升存量用户运营效能具有重要的实践意义。

### 1.2 国内外研究现状

在电信行业的用户分析与套餐推荐领域，国内外的研究和实践主要集中在以下几个方向：

**基于协同过滤的推荐**：通过分析用户群体的消费行为相似性，为目标用户推荐相似用户选择的套餐。该方法在互联网内容推荐中应用广泛，但在电信套餐推荐场景中，由于用户选择行为数据获取困难，且套餐变更频率低，协同过滤的冷启动问题较为突出。

**基于内容的推荐**：根据用户的历史消费特征（如 ARPU 值、流量使用量、通话时长等）与套餐属性的匹配度进行推荐。该方法可解释性较强，适合电信场景，但需要领域专家设计匹配规则。

**基于机器学习的推荐**：利用用户画像数据训练分类或回归模型，预测用户对不同套餐的偏好。该方法需要大量标注数据，且模型的可解释性较差，在实际营销场景中难以让一线人员理解和信任。

**基于规则的推荐**：由业务专家定义推荐规则和评分标准，通过规则引擎执行推荐逻辑。该方法可解释性强、易于调整、不需要训练数据，非常适合业务规则明确的场景。

在国内运营商的实际应用中，基于规则的推荐系统因其可解释性和可控性，在一线营销场景中得到了较为广泛的应用。本文所设计的系统采用基于规则的推荐方案，在保证推荐质量的同时，兼顾了系统的可理解性和可维护性。

### 1.3 研究内容与目标

本文的研究内容包括：

1. 分析存量用户套餐推荐的业务需求，设计系统的功能架构和技术架构
2. 设计并实现基于规则的推荐引擎，支持用户分群、多维度评分、备选方案生成
3. 开发前后端分离的 Web 应用系统，提供数据导入、推荐分析、人工校正、结果导出等完整功能
4. 集成 AI 大语言模型，实现智能营销话术生成
5. 对系统进行功能测试和性能验证

研究目标是构建一套实用、高效、易用的套餐推荐工具，帮助一线营销人员提升工作效率和推荐准确性。

### 1.4 论文组织结构

本文共分为七章。第一章为绪论，介绍研究背景、现状和目标。第二章介绍系统所涉及的相关技术。第三章进行系统需求分析。第四章进行系统总体设计和详细设计。第五章描述系统的具体实现过程。第六章进行系统测试与验证。第七章为总结与展望。

---

## 第二章 相关技术介绍

### 2.1 前端技术

**React 19** 是 Meta 开源的 JavaScript 用户界面库，采用组件化开发思想，通过虚拟 DOM 技术实现高效的页面更新。React 19 引入了 Server Components、Actions 等新特性，进一步提升了应用性能和开发体验。在本系统中，React 用于构建所有的页面组件，包括数据导入、分析看板、套餐管理、用户详情等功能模块。

**TypeScript** 是 JavaScript 的超集，增加了静态类型系统。TypeScript 能够在开发阶段捕获类型错误，提供更好的代码补全和重构支持，特别适合中大型前端项目的开发。本系统使用 TypeScript 5.8 进行开发，通过类型定义确保前后端数据交互的一致性。

**Vite** 是新一代前端构建工具，利用浏览器原生 ES Module 实现快速的开发服务器启动和热更新。相比传统的 Webpack，Vite 在开发阶段的启动速度有显著提升。本系统使用 Vite 6.2 作为构建工具，并通过其内置的代理功能将 API 请求转发到后端服务。

**Recharts** 是基于 React 的图表库，封装了 D3.js 的图表能力，提供了声明式的组件 API。本系统使用 Recharts 实现分析看板中的饼图和柱状图，用于展示套餐分布和用户对比数据。

**Tailwind CSS** 是原子化 CSS 框架，通过预定义的工具类快速构建界面样式。本系统通过 CDN 方式引入 Tailwind CSS，避免了复杂的构建配置，同时保持了良好的样式一致性。

### 2.2 后端技术

**Python** 是广泛使用的高级编程语言，以其简洁的语法和丰富的生态系统著称。在 Web 后端开发、数据分析、人工智能等领域有着广泛的应用。

**FastAPI** 是基于 Python 的现代 Web 框架，具有以下优势：
- 基于标准 Python 类型提示，自动生成 API 文档
- 原生支持异步请求处理，性能优异
- 内置 Pydantic 数据校验，确保请求数据的正确性
- 依赖注入系统，便于管理复杂的服务依赖关系

**SQLAlchemy** 是 Python 生态中最流行的 ORM（对象关系映射）框架，支持多种数据库后端。本系统使用 SQLAlchemy 2.0 的声明式模型定义方式，将数据库表映射为 Python 类，简化了数据库操作。

**Pydantic** 是 Python 的数据校验库，基于类型注解实现数据的自动校验和序列化。在 FastAPI 中，Pydantic 用于定义请求体和响应体的数据结构，确保 API 数据交换的规范性。

### 2.3 数据库技术

**SQLite** 是轻量级的嵌入式关系数据库，不需要独立的数据库服务器进程，数据存储在单个文件中。选择 SQLite 作为本系统的数据库方案，主要基于以下考虑：

1. **部署简便**：无需安装和配置独立的数据库服务，降低部署门槛
2. **适合单机场景**：本系统面向单个营销团队或个人使用，SQLite 的并发能力完全满足需求
3. **数据量适中**：单次推荐的用户量通常在百至千级别，SQLite 的性能绰绰有余
4. **跨平台兼容**：SQLite 数据库文件可在不同操作系统间无缝迁移

为兼容 exFAT 文件系统（如外接移动硬盘），系统在启动时设置 `PRAGMA journal_mode=OFF`，禁用 SQLite 的预写日志模式。

### 2.4 AI 集成技术

大语言模型（LLM）的快速发展为自然语言生成任务提供了强大的能力。本系统通过 API 调用方式集成外部 AI 服务，支持三种协议：

1. **OpenAI Responses API**：OpenAI 最新的响应式 API 接口
2. **OpenAI Chat Completions API**：业界广泛采用的对话补全接口，兼容大多数第三方网关
3. **Anthropic Messages API**：Anthropic Claude 系列模型的原生接口

系统使用 `httpx` 异步 HTTP 客户端进行 API 调用，通过统一的适配器模式屏蔽不同提供商的接口差异，实现了灵活的 AI 服务切换能力。

---

## 第三章 系统需求分析

### 3.1 业务需求分析

存量用户套餐推荐的核心业务流程如下：

1. **数据准备阶段**：从公司 CRM/BSS 系统导出存量用户数据，包括用户基本信息、当前套餐信息、近三个月消费数据、流量和语音使用情况等
2. **套餐维护阶段**：维护当前可售套餐目录，确保套餐信息（资费、流量、语音、宽带等）与最新政策一致
3. **智能推荐阶段**：对批量用户数据进行分析，根据用户特征和消费行为，自动匹配最优套餐方案
4. **人工审核阶段**：营销主管或一线人员对推荐结果进行逐条审核，结合实际情况进行人工校正
5. **外呼营销阶段**：根据确认后的推荐结果和营销话术，进行电话回访和推荐
6. **结果追踪阶段**：记录外呼结果，跟踪推荐转化率

### 3.2 功能需求分析

#### 3.2.1 套餐管理需求

- 系统内置当前在售套餐数据，支持查看、新增、编辑、删除操作
- 支持通过 Excel 模板批量导入套餐数据，导入方式为全量覆盖
- 支持套餐上下架管理
- 提供套餐导入标准模板下载

#### 3.2.2 用户数据导入需求

- 支持通过 Excel 模板批量导入用户清单
- 自动识别中文字段名并映射到系统字段
- 支持必填字段和可选字段的灵活配置
- 导入过程中进行数据校验，提示异常数据

#### 3.2.3 智能推荐需求

- 根据用户当前套餐信息自动判断用户所属分群（移动用户/宽带用户/FTTR用户）
- 在同分群套餐中筛选候选方案，执行不降档规则
- 根据用户消费特征控制提档幅度
- 对候选套餐进行多维度评分排序，输出主推荐方案和备选方案
- 自动生成推荐理由和风险等级评估
- 基于模板生成外呼营销话术

#### 3.2.4 人工校正需求

- 支持查看每个用户的详细推荐信息
- 支持从全部在售套餐中手动选择最终推荐方案
- 支持设置审核状态（待确认/已接受/已驳回）
- 支持填写审核备注

#### 3.2.5 AI 话术生成需求

- 支持配置 AI 提供商（协议类型、地址、API Key、模型）
- 支持连接测试功能
- 根据用户画像和推荐结果生成自然语言营销话术

#### 3.2.6 结果导出需求

- 支持将推荐结果导出为 Excel 文件
- 导出内容包括用户信息、推荐结果、人工校正、审核状态、AI 话术等完整信息

### 3.3 非功能需求分析

1. **易用性**：界面简洁直观，操作流程清晰，非技术用户可快速上手
2. **性能**：百级用户数据导入和推荐计算应在秒级完成
3. **可靠性**：数据导入时进行充分校验，避免脏数据进入系统
4. **可扩展性**：架构设计支持后续功能扩展，如推荐策略可配置化、结果追踪等

### 3.4 用例分析

系统涉及的主要用户角色和操作用例如下：

| 角色 | 主要用例 |
|------|----------|
| 营销人员 | 导入用户数据、查看推荐结果、人工校正、导出报表、生成话术 |
| 营销主管 | 维护套餐目录、审核推荐结果、查看分析看板 |
| 系统管理员 | 配置 AI 提供商、系统部署与维护 |

---

## 第四章 系统设计

### 4.1 系统总体架构设计

本系统采用前后端分离的 B/S（Browser/Server）架构，整体分为三层：

```
┌─────────────────────────────────────────────────┐
│                   表现层 (Frontend)               │
│  React 19 + TypeScript + Tailwind CSS + Recharts │
│  Vite 开发服务器 (port 3000)                      │
└───────────────────────┬─────────────────────────┘
                        │ HTTP / REST API
┌───────────────────────┴─────────────────────────┐
│                 业务逻辑层 (Backend)              │
│  FastAPI + 推荐引擎 + AI 服务                     │
│  Uvicorn ASGI 服务器 (port 8000)                  │
└───────────────────────┬─────────────────────────┘
                        │ SQLAlchemy ORM
┌───────────────────────┴─────────────────────────┐
│                  数据访问层 (Database)             │
│  SQLite (tariff.db)                              │
└─────────────────────────────────────────────────┘
```

前端通过 Vite 的代理功能将 `/api` 路径的请求转发到后端 8000 端口，实现了开发阶段的前后端联调。生产部署时，可通过 Nginx 等反向代理统一处理前端静态文件和后端 API 请求。

### 4.2 功能模块设计

#### 4.2.1 套餐管理模块

套餐管理模块负责维护系统中的套餐目录数据。核心功能包括：

- **套餐 CRUD**：通过 RESTful API 实现套餐的增删改查
- **Excel 批量导入**：支持通过 Excel 文件全量导入套餐数据，导入时自动校验字段完整性、数据合法性和编码唯一性
- **全量覆盖策略**：导入成功后替换当前全部套餐数据，避免增量合并带来的数据不一致问题

#### 4.2.2 用户数据导入模块

用户数据导入模块负责解析用户上传的 Excel 文件：

- **字段映射**：系统定义了中文表头到内部字段名的映射关系，支持自动识别
- **数据归一化**：对导入数据进行标准化处理，如饱和度支持 0-1 小数和 0-100 整数百分比两种格式
- **批次管理**：每次导入生成唯一的 `batch_id`，后续的推荐操作基于批次进行

#### 4.2.3 推荐引擎模块

推荐引擎是系统的核心模块，采用多步骤的规则化推荐流程：

**Step 1：数据归一化**
对用户导入数据进行标准化处理，包括数值类型转换、缺失值填充、异常值修正等。

**Step 2：用户分群**
根据用户的当前套餐名称、套餐类型、是否有宽带、是否 FTTR 等信息，将用户分为三个群组：
- `mobile`：纯移动用户，当前套餐不含宽带
- `broadband`：宽带融合用户，当前套餐包含宽带但非 FTTR
- `fttr`：全光 WiFi 用户，当前套餐包含 FTTR

**Step 3：候选筛选**
在用户所属分群的可售套餐中筛选候选方案：
- 排除已下架套餐
- 执行"不降档"规则：推荐套餐的资费不低于当前套餐
- 宽带用户保留宽带权益：不推荐无宽带套餐给宽带用户
- FTTR 用户保留 FTTR 权益

**Step 4：价格跳档幅度控制**
根据用户分群和使用饱和度设置最大允许提档幅度：
- 饱和度较低的用户：允许较大幅度提档
- 饱和度较高的用户：限制提档幅度，降低推荐风险

**Step 5：多维度评分**
对候选套餐进行加权评分，评分维度包括：
- **价格差**：推荐套餐与当前套餐的月费差异
- **资源覆盖**：流量和语音资源对用户使用需求的满足程度
- **资源浪费**：推荐套餐资源超出用户实际需求的程度
- **宽带速率**：宽带用户场景下的速率提升幅度

**Step 6：结果排序与输出**
按评分排序，取最高分作为主推荐方案，次高分的最多 3 个作为备选方案。同时生成推荐理由文本、风险等级（低/中/高）、预计账单金额和营销话术。

#### 4.2.4 人工校正与审核模块

人工校正模块允许营销人员对系统推荐结果进行调整：
- 从全部在售套餐中选择最终推荐方案
- 设置审核状态：待确认 → 已接受 / 已驳回
- 填写审核备注，记录校正原因
- 系统自动标记是否经过人工校正

#### 4.2.5 AI 话术生成模块

AI 话术模块采用适配器模式设计，通过统一的接口支持多种 AI 提供商：
- 用户在前端配置 AI 提供商信息（协议、地址、API Key、模型）
- 系统构造包含用户画像和推荐结果的 Prompt
- 调用 AI API 生成自然语言营销话术
- 话术支持在前端编辑和保存

#### 4.2.6 数据分析看板模块

分析看板提供全局视角的数据展示：
- 统计卡片：总用户数、推荐结果分布等关键指标
- 套餐分布饼图：展示各套餐的推荐占比
- 结果列表：支持按地区筛选、按手机号/套餐搜索
- 一键导出功能

### 4.3 数据库设计

系统使用 SQLAlchemy ORM 定义了以下核心数据模型：

**Plan 表（套餐表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 套餐编码（主键） |
| name | String | 套餐名称 |
| price | Float | 月费 |
| data | Float | 流量（GB） |
| voice | Float | 语音（分钟） |
| has_broadband | Boolean | 是否含宽带 |
| broadband_speed | Float | 宽带速率（Mbps） |
| is_fttr | Boolean | 是否 FTTR |
| is_active | Boolean | 是否上架 |
| extras | String | 其他权益 |

**UserRecord 表（用户记录表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| batch_id | String | 批次号 |
| phone | String | 联系电话 |
| location | String | 归属地 |
| plan_name | String | 当前套餐名称 |
| price | Float | 当前档位 |
| arpu | Float | 近三月 ARPU |
| data_usage | Float | 流量使用量 |
| voice_usage | Float | 通话使用量 |
| data_saturation | Float | 流量饱和度 |
| voice_saturation | Float | 语音饱和度 |
| over_amount | Float | 超套金额 |
| has_broadband | Boolean | 是否有宽带 |
| broadband_speed | Float | 宽带速率 |
| is_fttr | Boolean | 是否 FTTR |

**RecommendationResult 表（推荐结果表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| batch_id | String | 批次号 |
| user_record_id | Integer | 关联用户记录 |
| current_plan_id | String | 当前套餐 ID |
| recommended_plan_id | String | 系统推荐套餐 ID |
| final_plan_id | String | 最终确认套餐 ID |
| reason | String | 推荐理由 |
| risk_level | String | 风险等级 |
| predicted_bill | Float | 预计账单 |
| predicted_saving | Float | 预计节省 |
| script | String | 营销话术 |
| ai_script | String | AI 生成话术 |
| review_status | String | 审核状态 |
| review_note | String | 审核备注 |
| is_manual | Boolean | 是否人工校正 |
| selection_mode | String | 选择模式 |

**ResultAlternative 表（备选方案表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| result_id | Integer | 关联推荐结果 |
| plan_id | String | 备选套餐 ID |
| rank | Integer | 排序序号 |

**AIConfig 表（AI 配置表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| provider | String | 提供商名称 |
| protocol | String | 协议类型 |
| base_url | String | 基础地址 |
| endpoint | String | 接口路径 |
| api_key | String | API Key |
| model | String | 模型名称 |
| enabled | Boolean | 是否启用 |

### 4.4 API 接口设计

系统后端提供以下 RESTful API 接口：

**套餐管理 `/api/v1/plans`**
- `GET /` — 获取套餐列表
- `POST /` — 新增套餐
- `PUT /{id}` — 更新套餐
- `DELETE /{id}` — 删除套餐
- `POST /import` — Excel 批量导入

**用户导入 `/api/v1/users`**
- `POST /import` — 批量导入用户记录

**推荐结果 `/api/v1/recommendations`**
- `POST /run` — 运行推荐引擎
- `GET /` — 获取推荐结果列表
- `GET /{id}` — 获取单条推荐详情
- `PUT /{id}` — 更新推荐结果
- `POST /recompute` — 重新计算推荐
- `GET /export` — 导出 Excel

**AI 服务 `/api/v1/ai`**
- `GET /config` — 获取 AI 配置
- `POST /config` — 保存 AI 配置
- `POST /test` — 测试 AI 连接
- `POST /generate` — 生成 AI 话术

---

## 第五章 系统实现

### 5.1 开发环境搭建

**前端开发环境**：
- 操作系统：macOS / Windows / Linux
- 运行时：Node.js 20.x LTS
- 包管理器：npm 10.x
- IDE：WebStorm / VS Code

**后端开发环境**：
- 语言版本：Python 3.12
- 虚拟环境：venv
- IDE：PyCharm / VS Code

开发阶段通过两个终端分别启动前端和后端服务：
- 前端：`npm run dev` 启动 Vite 开发服务器（端口 3000）
- 后端：`uvicorn app.main:app --reload` 启动 FastAPI 开发服务器（端口 8000）

### 5.2 后端核心实现

#### 5.2.1 FastAPI 应用初始化

后端入口文件 `main.py` 完成以下初始化工作：

1. 设置 SQLite 的 `journal_mode=OFF` 以兼容 exFAT 文件系统
2. 调用 `Base.metadata.create_all()` 创建数据库表
3. 执行 `seed_plans()` 检查并写入默认套餐种子数据（30 款套餐）
4. 创建 FastAPI 应用实例，配置 CORS 中间件允许跨域请求
5. 注册四个路由模块到 `/api/v1` 路径前缀下

```python
app = FastAPI(title="存量用户套餐推荐系统 API", version="1.0.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(plans.router, prefix="/api/v1")
app.include_router(users.router, prefix="/api/v1")
app.include_router(recommendations.router, prefix="/api/v1")
app.include_router(ai.router, prefix="/api/v1")
```

#### 5.2.2 推荐引擎实现

推荐引擎的核心逻辑实现在 `services/engine.py` 中，主要包含以下函数：

**用户分群函数**：根据套餐名称关键词（如"全家享""全光"）、`has_broadband` 和 `is_fttr` 字段推断用户所属分群。

**候选筛选函数**：按分群过滤可售套餐，执行不降档、保留宽带/FTTR 权益等规则。

**评分函数**：对每个候选套餐计算综合得分，公式考虑以下因素：
- 价格差得分：价格差越小得分越高
- 资源覆盖得分：流量和语音满足用户需求的程度
- 资源浪费得分：超出用户需求过多时扣分
- 宽带速率得分：宽带场景下的速率提升加分

**话术生成函数**：根据推荐结果和用户特征，通过模板拼接生成外呼营销话术。

#### 5.2.3 AI 服务实现

AI 服务实现在 `services/ai_service.py` 中，采用统一的调用接口支持三种协议。核心流程：

1. 根据配置的协议类型选择对应的 API 调用方式
2. 构造包含用户画像、当前套餐、推荐套餐信息的 Prompt
3. 发送异步 HTTP 请求到 AI 提供商
4. 解析响应并提取生成的营销话术

#### 5.2.4 数据导入与导出实现

数据导入使用 `openpyxl` 库解析上传的 Excel 文件，通过 SQLAlchemy 的批量写入优化导入性能。导出功能使用 `openpyxl` 生成包含完整推荐结果的 Excel 文件，设置合理的列宽和表头样式。

### 5.3 前端核心实现

#### 5.3.1 应用架构与路由

前端采用状态驱动的页面导航方式，通过 `PageView` 联合类型定义页面状态：

```typescript
type PageView =
  | { page: 'home' }
  | { page: 'import' }
  | { page: 'dashboard'; batchId: string }
  | { page: 'plans' }
  | { page: 'detail'; resultId: number; batchId: string }
  | { page: 'ai-settings' };
```

`App.tsx` 根据当前 `PageView` 状态渲染对应的组件，实现了无依赖的轻量级路由方案。

#### 5.3.2 数据导入与解析

前端使用 SheetJS 库在浏览器端解析用户上传的 Excel 文件：
- 读取第一个工作表
- 自动识别中文表头并映射到内部字段名
- 对必填字段进行缺失检查
- 将解析后的数据发送到后端 API

#### 5.3.3 分析看板实现

分析看板使用 Recharts 实现数据可视化：
- 使用 `PieChart` 展示套餐推荐分布
- 使用统计卡片展示总用户数、各状态数量等关键指标
- 实现按地区下拉筛选和关键词搜索功能
- 支持一键导出 Excel 报表

#### 5.3.4 用户详情与人工校正

用户详情页实现了以下交互：
- 当前套餐与推荐套餐的多维度对比展示（资费、流量、语音、宽带）
- Recharts 柱状图可视化资源对比
- 套餐下拉选择器，支持从全部在售套餐中改选最终方案
- 审核状态切换按钮组
- 备注输入框
- AI 话术生成按钮和话术展示区域

### 5.4 前后端联调

前端通过 Vite 配置的代理实现与后端的联调：

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
},
```

前端 API 客户端（`services/api.ts`）封装了所有后端接口调用，统一使用 `/api/v1` 路径前缀，并处理响应状态码和错误信息。

---

## 第六章 系统测试

### 6.1 测试策略

系统测试采用以下策略：
- 功能测试：验证各模块功能的正确性
- 集成测试：验证前后端数据交互的一致性
- 性能测试：验证批量数据处理的响应时间
- 兼容性测试：验证跨浏览器和跨操作系统的兼容性

### 6.2 功能测试

#### 6.2.1 套餐管理测试

| 测试项 | 预期结果 | 测试结果 |
|--------|----------|----------|
| 查看套餐列表 | 正确显示 30 款默认套餐 | 通过 |
| 新增套餐 | 套餐成功添加到列表 | 通过 |
| 编辑套餐 | 套餐信息正确更新 | 通过 |
| 删除套餐 | 套餐从列表中移除 | 通过 |
| Excel 导入 | 正确解析并替换套餐数据 | 通过 |
| 导入校验 | 缺失字段和异常数据被正确提示 | 通过 |

#### 6.2.2 用户导入与推荐测试

| 测试项 | 预期结果 | 测试结果 |
|--------|----------|----------|
| Excel 用户导入 | 正确解析所有字段 | 通过 |
| 自动推荐 | 每个用户获得主推荐和备选方案 | 通过 |
| 推荐理由 | 推荐理由准确反映推荐逻辑 | 通过 |
| 风险等级 | 风险等级与提档幅度匹配 | 通过 |

#### 6.2.3 人工校正测试

| 测试项 | 预期结果 | 测试结果 |
|--------|----------|----------|
| 改选套餐 | 最终推荐正确更新 | 通过 |
| 设置审核状态 | 状态正确切换 | 通过 |
| 导出结果 | Excel 包含人工校正后的内容 | 通过 |

### 6.3 性能测试

| 测试场景 | 数据量 | 耗时 | 结论 |
|----------|--------|------|------|
| 用户导入 | 100 条 | < 2 秒 | 满足要求 |
| 推荐计算 | 100 条 | < 3 秒 | 满足要求 |
| 结果导出 | 100 条 | < 2 秒 | 满足要求 |

### 6.4 兼容性测试

| 浏览器 | 版本 | 测试结果 |
|--------|------|----------|
| Chrome | 120+ | 通过 |
| Safari | 17+ | 通过 |
| Firefox | 120+ | 通过 |
| Edge | 120+ | 通过 |

跨操作系统测试：macOS、Windows、Linux 均可正常部署和运行。

### 6.5 测试结果与分析

经过全面的功能测试、性能测试和兼容性测试，系统各项功能运行正常，满足设计要求。推荐引擎的推荐结果符合业务逻辑，人工校正流程顺畅，AI 话术生成功能稳定可用。百级数据量的处理性能完全满足一线营销场景的需求。

---

## 第七章 总结与展望

### 7.1 工作总结

本文针对中国移动存量用户运营的实际需求，设计并实现了一套基于规则引擎的套餐推荐系统。主要完成了以下工作：

1. **完成了系统的需求分析和架构设计**：采用前后端分离架构，前端使用 React + TypeScript，后端使用 FastAPI + SQLAlchemy，数据层使用 SQLite，技术选型合理，适合中小规模的内部工具场景

2. **实现了基于规则的推荐引擎**：通过用户分群、候选筛选、多维度评分等算法，实现了自动化的套餐推荐。推荐结果可解释性强，便于一线人员理解和信任

3. **开发了完整的业务功能**：包括套餐管理、数据导入、智能推荐、人工校正、审核管理、AI 话术生成和结果导出，覆盖了存量用户运营的核心流程

4. **集成了 AI 大语言模型能力**：通过适配器模式支持多种 AI 提供商，实现了智能营销话术生成，提升了外呼营销的专业性和效率

5. **系统已在实际业务中投入使用**：在昌吉分公司一线营销团队中进行了应用验证，有效提升了套餐推荐的准确性和工作效率

### 7.2 创新点

1. **规则引擎与人工校正的混合决策模式**：系统推荐结果并非最终结论，而是为人工决策提供参考。营销人员可以在系统推荐的基础上进行人工校正，形成"机器推荐 + 人工把关"的混合决策模式，兼顾了效率和准确性

2. **多维度加权评分的推荐算法**：综合考虑价格差、资源覆盖、资源浪费、宽带速率等多个维度，通过加权评分实现套餐的综合排序，避免了单一维度决策的局限性

3. **AI 话术与规则话术的双轨制**：系统同时提供基于模板的规则话术和基于大模型的 AI 话术，前者保证基础质量，后者提供更自然的表达，用户可根据实际需要选择使用

### 7.3 不足与改进方向

当前系统存在以下不足和改进空间：

1. **推荐策略硬编码**：当前推荐规则和评分权重写死在代码中，后续应实现策略配置化，支持运营人员通过界面调整推荐参数

2. **缺乏结果追踪**：系统目前只覆盖了推荐和导出环节，缺乏对外呼结果和推荐转化率的追踪，难以评估推荐策略的实际效果

3. **套餐库管理简单**：当前采用全量覆盖的导入方式，缺乏版本管理和变更追踪，后续应增加套餐版本化功能

4. **未接入业务系统**：系统目前独立运行，未与公司 CRM、BSS、宽带资源等业务系统对接，数据需要人工导出和导入

5. **推荐算法可进一步优化**：当前基于规则的推荐在用户量和场景复杂度增加时可能面临瓶颈，后续可探索引入机器学习推荐算法，提升推荐精度

未来的发展方向是将系统从"辅助分析工具"逐步演进为"智能化运营平台"，实现推荐策略可配置、业务结果可追踪、系统数据可对接的完整业务闭环。

---

## 致谢

在试用期的工作中，衷心感谢公司领导和同事们的悉心指导与大力支持。感谢部门领导在项目方向上的指导和资源支持，感谢同事在业务理解和系统测试方面的帮助。正是在大家的帮助下，本项目才能顺利完成并投入实际使用。

在今后的工作中，我将继续努力提升专业能力，为公司的数字化转型和存量用户运营贡献更多力量。

---

## 参考文献

[1] 工业和信息化部. 2025年通信业统计公报[R]. 北京: 工业和信息化部, 2026.

[2] 项亮. 推荐系统实践[M]. 北京: 人民邮电出版社, 2012.

[3] FastAPI Documentation[EB/OL]. https://fastapi.tiangolo.com/.

[4] React Documentation[EB/OL]. https://react.dev/.

[5] SQLAlchemy Documentation[EB/OL]. https://docs.sqlalchemy.org/.

[6] 张亮, 李明. 基于大数据的电信用户套餐推荐系统研究[J]. 电信科学, 2023, 39(5): 112-118.

[7] 王伟, 赵强. 存量用户精细化运营策略研究[J]. 移动通信, 2024, 48(3): 45-50.

[8] OpenAI. GPT-4 Technical Report[R]. 2023.

[9] Anthropic. Claude Technical Report[R]. 2024.

[10] Vite Documentation[EB/OL]. https://vitejs.dev/.
